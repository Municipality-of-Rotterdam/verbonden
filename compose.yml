name: verbonden
services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: verbonden
      POSTGRES_USER: verbonden
      POSTGRES_PASSWORD: verbonden
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
