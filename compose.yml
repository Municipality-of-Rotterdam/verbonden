services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: huwelijk
      POSTGRES_USER: huwelijk
      POSTGRES_PASSWORD: huwelijk
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
